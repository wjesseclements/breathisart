import { describe, expect, it } from 'vitest';

// Placeholder until the breath engine lands in slice 2.
describe('toolchain smoke test', () => {
  it('runs', () => {
    expect(1 + 1).toBe(2);
  });
});
